import React from "react";
import { useEffect } from "react";

function NotFound(){
    useEffect(() => {
      window.scrollTo(0, 0);
    }, []);
    
    return(
        <main>
            
        </main>
    );
}

export default NotFound